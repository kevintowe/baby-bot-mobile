import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'baby-bot-mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
